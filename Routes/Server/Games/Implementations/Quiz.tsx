import React from 'react'
/**
 *
 * Shows a board wrapped in a show players component.
 * This board has one main rectangle to show question, and below 4 rectangles with answers.
 * It has a timer that will show correct answer and calculate points, after short delay, ask game manager for next step.
 * It can be ended prematurely when all players give their answers.
 * There could be a component called question manager, that would render a question and would have API to load next question.
 * There is a constructor for the question manager that sets the topic, or initial difficulty.
 */

const Quiz = () => {
	return <></>
}
